spring:
  application:
    name: eureka-consumer
server:
  port: 9102
eureka:
  client:
    serviceUrl:
      defaultZone: http://root:root@localhost:8761/eureka/

feign:
  hystrix:
    enabled: true
